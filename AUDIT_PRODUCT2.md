# Mukdang product2 관리자 시스템 전수 감사

> 감사일: 2026-08-04  
> 대상 저장소: `jwhtws/product2` 현재 작업 트리  
> 목적: 운영자가 하루 30분 안에 Mukdang의 핵심 운영 업무를 완결할 수 있는지 평가  
> 범위 제한: 코드·UI·기능 변경 없이 정적 코드, 설정, 마이그레이션, 테스트, 워크플로를 감사한다.

## Executive Summary

**종합 점수: 54/100**

**30분 운영 가능 여부: 조건부 불가능**이다.

- 데이터와 사용자 활동을 **확인만 하는 정상 무사고일**에는 로그인, 대시보드, 신규 팝업 목록, 수집 결과, 리뷰와 로그를 약 20~30분 안에 훑을 수 있다.
- 그러나 신규 팝업 승인, 팝업 정보 수정, 수동 종료, 중복 병합, 브랜드/광고 문의, 사용자 신고 처리는 관리자 콘솔에 구현되어 있지 않다.
- 따라서 정보 오류나 문의가 한 건이라도 발생하는 실제 운영일에는 product1 원본 또는 GitHub 등 콘솔 밖의 수단으로 이동해야 하며, product2 안에서 업무를 완결하거나 처리 이력을 남길 수 없다.
- 현재 시스템의 정체성은 **운영 콘솔**이라기보다 **모니터링 대시보드 + 회원/리뷰 관리 도구**에 가깝다.

### 판정 기준

“하루 30분 운영”은 단순 열람이 아니라 다음을 한 번의 운영 루틴에서 수행할 수 있다는 뜻으로 평가했다.

1. 오늘 처리할 예외를 한 화면에서 발견한다.
2. 근거와 원본을 확인한다.
3. 승인·수정·종료·병합·답변·숨김 등 필요한 조치를 한다.
4. 결과가 product1에 반영되었는지 확인한다.
5. 담당자, 사유, 이전/이후 값과 처리 시간이 기록된다.

현재는 1~2번을 일부 지원하고, 3~5번은 도메인별 편차가 크다.

### 점수 구성

| 평가 영역 | 배점 | 점수 | 판단 |
| --- | ---: | ---: | --- |
| 기능 완결성 | 25 | 11 | 조회 기능은 넓지만 팝업·문의·신고의 핵심 쓰기 흐름 부재 |
| 운영 효율 | 20 | 8 | 무사고일 점검은 가능, 예외 처리와 통합 작업함 부재 |
| UX·정보 구조 | 15 | 9 | 일관된 화면과 검색은 장점, 12개 평면 메뉴와 긴 표가 부담 |
| product1·데이터 연동 | 15 | 10 | D1과 정적 보고서 연동은 실재, 데이터 진실 원천이 분산 |
| 보안·감사성 | 15 | 9 | Secrets, HMAC 쿠키, rate limit, 보안 헤더는 양호; 단일 공유 코드·RBAC 부재 |
| 테스트·운영 복구성 | 10 | 7 | 입력 헬퍼와 배포 검사는 있으나 API/권한/통합 테스트와 롤백 이력 부족 |
| **합계** | **100** | **54** | **모니터링 가능, 핵심 운영 완결 불가** |

---

## 감사 범위와 근거

다음 파일을 기준으로 평가했다.

- 관리자 UI: `ops-7c4e91b6.html`, `admin.js`, `admin.css`
- 관리자 API: `functions/api/admin/[[path]].js`
- 인증·공통 API 보안: `functions/_lib/auth.js`, `functions/api/_middleware.js`, `functions/_middleware.js`, `_headers`
- 쿼리·페이지네이션: `functions/_lib/admin-query.js`
- D1: `wrangler.toml`, `migrations/*.sql`
- 연동·자동화: `.github/workflows/restaurant-change-history.yml`, `scripts/build-restaurant-change-history.mjs`
- 검증: `scripts/validate-project.mjs`, `tests/admin-query.test.mjs`
- 책임 경계: `ARCHITECTURE.md`

이번 감사는 실행 중인 Cloudflare 환경과 product1 저장소 내부를 직접 감사한 것이 아니다. 따라서 Secret의 실제 설정 여부, D1 production 데이터의 품질, GitHub Actions의 실제 성공률, product1의 쓰기 경로는 코드에 나타난 계약만으로 판단했다. 현재 작업 트리의 미커밋 `admin.js`/`admin.css` 변경도 실제 감사 대상에 포함했으며 수정하지 않았다.

---

## 1. 현재 관리자 기능 목록

상태 표기:

- **완료:** 해당 목적의 조회 또는 처리를 콘솔에서 끝낼 수 있음
- **부분:** 일부 조회·처리만 가능하거나 외부 시스템 의존
- **미구현:** 메뉴/API/처리 흐름이 없음

### 기능별 평가표

| 영역 | 목적 | 현재 구현 상태 | 예상 사용 빈도 | 유지 여부 | 개선 필요 여부 |
| --- | --- | --- | --- | --- | --- |
| Dashboard | 회원·리뷰·활동·식당 규모와 서버 상태의 빠른 확인 | **부분.** D1 집계 5종을 표시한다. 식당 메타는 product1 정적 JSON을 브라우저에서 별도 로드한다. Pages Functions, D1, 쿠키, 식당 원본의 “정상” 표시는 실제 health check가 아니라 고정 문구다. 팝업, 실패 작업, 신고, 검수 대기는 없다. | 매일 1회 | 유지 | **P0 개선.** 오늘의 예외·작업 큐 중심으로 재설계 필요 |
| Popup 관리 | 수집된 푸드 팝업 확인, 상태/지역/브랜드별 탐색, 수집 실행 | **부분.** product1 `data/popups.json`을 GitHub API 우선·배포 JSON fallback으로 읽고, 진행/예정/종료·검색·정렬·지역/브랜드 필터, 신규 수집 묶음, 원문 링크, workflow 실행을 제공한다. 생성·승인·편집·종료·숨김·병합 API는 없다. D1 `food_popups` 테이블도 이 화면에서 사용하지 않는다. | 매일 | 반드시 유지 | **최우선 개선.** 읽기 전용 모니터를 검수·정정 가능한 운영 큐로 전환 필요 |
| 회원 관리 | 회원 검색, 상태·권한 관리, 삭제 | **완료에 가까운 부분.** 이름/이메일 검색, 50건 커서 페이지, 활성/정지, member/admin 권한 변경, 물리 삭제가 가능하다. 변경 사유, 상세, 제재 기간, 삭제 영향 미리보기, 개별 관리자 권한은 없다. | 주 1~수회, 신고 발생 시 | 유지 | **P0/P1 개선.** 물리 삭제 안전성, 사유·이력, 권한 상승 통제 필요 |
| 리뷰 관리 | 리뷰 검색, 숨김/공개, 삭제, 작성 제한 설정 | **부분.** 작성자/식당/내용 검색, 숨김 토글, 물리 삭제, 회원·식당별 일일 제한과 동일 내용 차단 설정이 있다. 신고 큐, 사유, 상세 문맥, 일괄 처리, 복구는 없다. | 매일 또는 신고 시 | 유지 | **P0 개선.** 신고 중심 큐, soft delete, 사유와 이의 처리 필요 |
| 운영 로그 | 관리자 작업 확인 | **부분.** `admin_logs`를 최신순 50건 커서 방식으로 보여준다. 로그인, 설정, 회원/리뷰/할 일, 수동 수집 실행을 기록한다. 관리자 식별자와 이전/이후 값, 대상 링크, 검색/필터, export, 실패 조작 로그는 없다. | 매일 짧게, 사고 시 집중 | 유지 | **P0 개선.** 감사 로그로서의 증거력이 부족 |
| 데이터 분석 | 회원·리뷰·행동·식당 변화와 기간 추이 확인 | **부분.** 일/주/월/년별 신규 회원·리뷰·활동, 식당 추가/제거, 사용자 행동과 상위 회원을 제공한다. 체류시간은 미수집임을 표시한다. 전환·팝업 행동·운영 SLA·품질 지표는 없다. | 주 1회 중심 | 유지하되 통합 | **P1 개선.** 일일 운영 메뉴가 아니라 주간 분석 그룹으로 이동 |
| 검색 | 회원·리뷰·사용자 데이터·팝업 검색과 검색어 순위 확인 | **부분.** 영역별 검색은 있으나 전역 검색은 없다. 검색 순위는 product1 `activity_events`의 `search` 이벤트를 기간별로 집계한다. 메뉴명 “검색 순위 관리”는 쓰기 기능이 없는 분석 화면이라 오해를 준다. | 영역 검색은 매일, 순위는 주 1회 | 유지하되 명칭·구조 변경 | **P1 개선.** 전역 검색과 “검색 분석”으로 명확화 |
| 설정 | 리뷰 등록 제한 관리 | **부분.** 별도 최상위 메뉴는 없고 리뷰 관리 내부 탭에서 3개 `service_settings`를 조회/수정한다. 전역 서비스 설정, 수집 설정, 알림, 관리자, 권한, feature flag는 없다. | 월 1회 이하 | 유지하되 하위 메뉴화 | **P1 개선.** 설정 변경 이력·권한·영향 설명 필요 |
| 식당 데이터 | 공공데이터 갱신·검증·변경 이력 확인 및 workflow 실행 | **부분.** product1의 regions/validation/refresh 보고서, GitHub workflow 최근 실행, product2의 90일 변경 이력을 결합한다. 실행 로그 링크와 수동 재실행이 있다. 오류 상세·warning 목록·담당/해결 상태는 없다. | 매일 1회 점검 | 유지 | **P0 개선.** 오류 건수에서 실제 오류 항목과 처리로 연결 필요 |
| 해야 할 일 | 운영 업무를 날짜·주·월·년 단위로 기록 | **완료.** 생성, 수정, 완료, 삭제, 우선순위, D1 저장과 감사 로그가 있다. 시스템 경보·문의·신고와 자동 연결되지 않는 수동 메모다. | 매일 | 조건부 유지 | **P1 개선.** 통합 운영 큐가 생기면 보조 메모로 축소 |
| 사용자 행동 분석 | 검색·저장·리스트·리뷰 행동과 활동 사용자 확인 | **부분.** 기간별 요약·추이·상위 50명을 제공한다. 체류시간 미수집, 팝업 상세·외부 클릭·저장 전환 등 핵심 퍼널이 없다. 사용자 이메일까지 넓게 노출한다. | 주 1회 | 유지하되 분석에 통합 | **P1 개선.** 목적·권한·개인정보 최소화 필요 |
| 사용자 데이터 | 프로필, 저장 맛집, 사용자 리스트 열람 | **부분.** 계정별 JSON 데이터를 파싱해 조회한다. 수정·삭제·export·개인정보 요청 처리 기능은 없다. 운영 목적이 불명확하고 개인정보 열람 범위가 넓다. | 문의/사고 시, 평소 낮음 | 제한적으로 유지 | **P1 개선.** 권한 제한, 접근 사유·감사, 계정 상세에 통합 |
| 검색·저장 활동 | product1 활동 이벤트 최신순 확인 | **부분.** 검색/save/list 이벤트와 회원/비회원 표시, 50건 페이지를 제공한다. 필터·검색·대상 링크·신고 처리 연계가 없다. 분석 메뉴와 내용이 중복된다. | 디버깅 시 | 통합 | **P1 개선.** “이벤트 탐색기” 또는 사용자 상세로 이동 |
| 리뷰 제한 설정 | 리뷰 남용 방지 정책 조정 | **완료.** 숫자 범위와 식당 제한≤전체 제한을 서버에서 검증하고 로그를 남긴다. | 드묾 | 유지 | **P1 개선.** 관리자 권한 분리와 변경 전후 값 필요 |
| 브랜드 문의 | 공식 정보 정정·등록·소유권 문의 처리 | **미구현** | 매일 또는 캠페인 시 | 신설 필요 | **P0** |
| 광고 문의 | 광고 리드 확인, 상태·담당·응답 관리 | **미구현** | 사업화 이후 매일/주간 | 신설 필요 | **P1** |
| 사용자 신고 | 팝업 오류·리뷰·회원 신고 접수와 처리 | **미구현** | 매일 | 신설 필요 | **P0** |

### 현재 메뉴 구조

실제 최상위 메뉴는 12개다.

1. 대시보드
2. 해야 할 일
3. 데이터 분석
4. 검색 순위 관리
5. 사용자 행동 분석
6. 회원 관리
7. 리뷰 관리
8. 사용자 데이터
9. 검색·저장 활동
10. 식당 데이터
11. 푸드 팝업 데이터
12. 운영 로그

최상위 메뉴에 “분석 3종”, “사용자 4종”, “데이터 2종”이 같은 깊이로 놓여 업무 우선순위가 드러나지 않는다. 반대로 실제 매일 처리해야 할 검수, 오류, 신고와 문의 메뉴는 없다.

---

## 2. 운영 시나리오 분석

### 시나리오 판정표

| 업무 | 가능 여부 | 현재 경로와 최소 클릭 | 완결성 | 판단 근거 |
| --- | --- | --- | --- | --- |
| 신규 팝업 승인 | **불가능** | 로그인(1) → 푸드 팝업 데이터(1) → 신규 목록/원문(1)까지 확인 가능 | 조회만 가능 | 승인 상태, 검수 큐, 승인 API, 초안→게시 전이가 없다. `popups.json` 수집 결과가 바로 읽기 모델이다. |
| 잘못된 정보 수정 | **불가능** | 푸드 팝업 데이터 → 검색/필터 → 원문 링크까지 2~4회 | 콘솔 밖 수정 필요 | 팝업 상세 편집 폼과 쓰기 API가 없다. `food_popups` D1 CRUD도 연결되지 않는다. |
| 종료 처리 | **자동 표시만 가능** | 푸드 팝업 데이터 → 종료 탭 2회 | 수동 예외 처리 불가 | UI가 날짜/상태로 종료를 계산해 표시하지만 연장, 조기 종료, 취소, 강제 종료를 기록할 수 없다. |
| 수집 오류 확인 | **부분 가능** | 식당/팝업 데이터 → 최근 결과 → GitHub 실행 로그 2~3회 | 탐지 가능, 해결은 외부 | workflow 결과, validation 요약, 실행 링크는 있다. 팝업은 파서 오류·누락·충돌 건수를 보여주지 않고, 식당도 error/warning 상세를 API에서 제거한다. |
| 중복 병합 | **불가능** | 경로 없음 | 불가 | 중복 탐지, 후보 비교, canonical 선택, 병합·복구 API가 없다. |
| 브랜드 문의 확인 | **불가능** | 경로 없음 | 불가 | 문의 데이터 모델, inbox, 상태, 담당, 답변, 브랜드 엔터티가 없다. |
| 광고 문의 확인 | **불가능** | 경로 없음 | 불가 | 광고 리드/캠페인/문의 메뉴와 API가 없다. |
| 사용자 신고 처리 | **불가능** | 리뷰/회원 직접 검색은 가능 | 신고 접수와 조치 연결 불가 | 신고 테이블·큐·사유·증거·처리 상태가 없다. 대상을 이미 알아야 숨김/정지할 수 있다. |

### 상세 시나리오

#### 신규 팝업 승인

현재 “새로 수집된 푸드팝업”은 `firstSeenAt` 최댓값과 같은 레코드를 묶어 보여준다. 신규 발견에는 유용하지만 다음이 없다.

- 승인 대기와 자동 게시의 구분
- 필수 필드 검증 결과
- 원본 대 정규값 비교
- 승인·반려·보류와 사유
- 승인자와 처리 시각
- product1 반영 확인

따라서 목록 확인은 5~10분 내 가능해도 승인 업무는 수행할 수 없다.

#### 잘못된 정보 수정 및 종료 처리

팝업 테이블은 출처로 이동할 수 있고 상태를 계산하지만 읽기 전용이다. 수정하려면 product1의 파일 또는 수집 로직을 별도로 찾아야 할 가능성이 높다. 이 외부 작업은 product2 감사 로그에 남지 않으며 다음 수집에서 수정이 유지되는지도 콘솔에서 알 수 없다.

#### 수집 오류 확인

- 식당: 최근 GitHub run, `validation-report.json`, `refresh-report.json`, regions manifest, 변경 이력을 읽는다.
- 팝업: 최근 `food-popup-refresh.yml` run과 `popups.json`을 읽는다.
- 공통 한계: 실패한 실행을 발견한 뒤 GitHub 로그로 이동해야 하며, 실패 원인·영향 레코드·재처리 상태를 product2에서 관리하지 않는다.

#### 문의 및 신고

브랜드, 광고, 사용자 신고를 위한 데이터 모델과 메뉴가 전혀 없다. 이메일이나 외부 채널을 사용한다면 현재 저장소에서는 그 존재, 담당자, SLA, 처리 이력을 확인할 수 없다. 이는 30분 운영 목표의 가장 큰 구조적 누락이다.

---

## 3. 관리자 UX 분석

### 클릭 수

클릭 수는 로그인 화면을 연 상태, 오류가 없고 첫 50건 안에서 대상을 찾는 기준이다.

| 작업 | 예상 클릭/입력 | 평가 |
| --- | ---: | --- |
| 로그인 후 대시보드 확인 | 코드 입력 + 로그인 1회 | 양호 |
| 팝업 신규/종료 현황 확인 | 메뉴 1회, 필요 시 탭 1회 | 양호 |
| 팝업 원문 확인 | 메뉴 1 + 원문 1 | 양호 |
| 팝업 수집 재실행 | 메뉴 1 + 실행 1 + 확인 1 | 양호하지만 결과 자동 추적 약함 |
| 회원 검색 후 정지 | 메뉴 1 + 검색 입력/변경 1 + 정지 1 | 3회 수준, 양호 |
| 리뷰 검색 후 숨김 | 메뉴 1 + 검색 1 + 숨김 1 | 3회 수준, 양호 |
| 리뷰 제한 변경 | 리뷰 1 + 설정 탭 1 + 저장 1 | 양호 |
| 식당 오류 확인 | 메뉴 1 + 필요 시 실행 로그 1 | 요약은 빠르나 상세는 외부 |
| 팝업 수정/승인/병합 | 경로 없음 | 치명적 |
| 신고/문의 처리 | 경로 없음 | 치명적 |

클릭 수 자체보다 **처리 가능한 목적지의 부재**가 문제다. 조회 업무는 1~3회로 짧지만, 핵심 운영 업무는 무한 클릭이 아니라 아예 경로가 없다.

### 불필요하거나 일일 운영 우선순위가 낮은 메뉴

- “데이터 분석”, “검색 순위 관리”, “사용자 행동 분석”은 매일 처리 메뉴가 아니라 주간 분석 그룹에 가깝다.
- “사용자 데이터”와 “검색·저장 활동”은 독립된 일일 메뉴보다 회원 상세 또는 진단 도구에 가깝다.
- “해야 할 일”은 유용하지만 시스템 경보·신고·문의와 연결되지 않아 별도의 수동 업무 목록을 하나 더 만든다.
- “검색 순위 관리”는 실제로 순위를 편집하지 않으므로 “검색 분석”이 정확하다.

### 중복 기능

- 데이터 분석의 활동 추이, 사용자 행동 분석, 검색 순위, 검색·저장 활동이 같은 `activity_events`를 서로 다른 형태로 보여준다.
- 회원 관리와 사용자 데이터가 사용자 단위 맥락을 나눠 보여준다.
- Dashboard의 식당 상태와 식당 데이터 화면이 겹치지만, Dashboard 값 일부는 고정 “정상” 문구라 신뢰 수준이 다르다.
- 팝업 화면 안에 전체 목록, 신규 목록, 종료 목록이 동시에 있어 같은 레코드가 한 화면에 중복 노출된다.

### 정보 구조

현재는 데이터 종류 중심의 평면 구조다. 30분 운영 목표에는 업무 상태 중심 구조가 더 적합하다.

```text
권장 논리 구조(향후 참고)
오늘의 운영
├─ 검수 대기
├─ 오류·충돌
├─ 신고·문의
└─ 곧 종료/실패 작업
데이터
├─ 팝업
└─ 식당
사용자 운영
├─ 회원
└─ 리뷰
분석
├─ 서비스 KPI
├─ 검색
└─ 사용자 행동
시스템
├─ 설정
├─ 관리자/권한
└─ 감사 로그
```

이번 감사에서는 UI를 변경하지 않으며, 위 구조는 개선 우선순위 판단을 위한 목표안이다.

### 모바일 사용성

장점:

- 900px 이하에서 사이드바가 햄버거 메뉴로 전환된다.
- 지표 카드와 주요 grid는 2열 또는 1열로 축소된다.
- 테이블과 차트는 가로 스크롤을 허용해 레이아웃 파손을 줄인다.
- 560px 이하에서 헤더와 로그가 세로로 바뀐다.

문제:

- 12개 메뉴가 245px 고정 사이드바 한 화면에 들어가며, 높이가 작은 모바일에서 메뉴 자체의 스크롤 규칙이 명확하지 않다.
- 메뉴를 열었을 때 backdrop, 바깥 클릭 닫기, Escape, 포커스 트랩이 없다.
- 표가 `white-space: nowrap`이라 팝업 8열, 회원 6열, 리뷰 7열을 모바일에서 계속 좌우 스크롤해야 한다.
- 행 작업 버튼이 표 오른쪽 끝에 있어 대상 정보와 작업을 함께 보기 어렵다.
- 메뉴 선택 시 사이드바는 닫히지만 긴 화면에서 상단 이동이나 현재 작업 요약이 없다.
- `prompt`/`confirm` 기반 작업은 할 일 수정 및 파괴 작업의 맥락과 접근성이 제한된다.
- 팝업 필터와 기간 탭이 많아 작은 화면에서 줄바꿈과 터치 이동이 커진다.

모바일은 **긴급 확인과 단일 조치**에는 쓸 수 있지만, 대량 검수와 데이터 비교에는 적합하지 않다.

---

## 4. product1 연동 분석

### 현재 연동되는 데이터

| 데이터 | 원천/경로 | product2 사용 방식 |
| --- | --- | --- |
| 회원 | 공용 D1 `users` | Dashboard 집계, 목록/검색, 상태·권한 변경, 삭제 |
| 리뷰 | 공용 D1 `reviews` | Dashboard·분석 집계, 목록/검색, 숨김, 삭제 |
| 사용자 프로필·저장·리스트 | 공용 D1 `user_data` | 회원별 JSON 파싱 조회 |
| 사용자 활동 | 공용 D1 `activity_events` | 최신 활동, 검색 순위, 기간 분석, 사용자 분석 |
| 서비스 설정 | 공용 D1 `service_settings` | 리뷰 제한 3종 조회·수정 |
| 관리자 로그인 실패 | 공용 D1 `auth_attempts` | IP 기준 rate limit 상태 저장 |
| 관리자 로그 | 공용 D1 `admin_logs` | 모든 구현된 관리 작업의 단순 action/detail 기록 |
| 관리자 할 일 | 공용 D1 `admin_tasks` | product2 전용 CRUD |
| 팝업 | product1 GitHub `data/popups.json`, 실패 시 배포 JSON | 읽기, 분류, 검색, workflow 상태/실행 |
| 식당 manifest | product1 배포 `data/restaurants/regions.json` | 규모·지역·업데이트 시각 조회 |
| 식당 검증 보고서 | product1 배포 `validation-report.json` | ok/stats/error·warning 수 요약 |
| 식당 갱신 보고서 | product1 배포 `refresh-report.json` | 최근 개업·폐업·갱신 시각 조회 |
| 식당 변경 이력 | product2 GitHub `data/restaurant-change-history.json` | 90일 요약과 날짜별 상세 조회, 분석 집계 |
| GitHub Actions | product1 workflows API | 최근 5개 실행 조회, 토큰이 있으면 2개 workflow 수동 dispatch |

### 누락된 데이터

- 팝업 승인·검수 상태, 검수자, 반려 사유
- 팝업 필드별 변경 이력과 수동 override
- 팝업 중복 후보·병합 관계·canonical ID
- 수집 원본 스냅샷, 파서 버전, 필드 충돌, 실패 레코드
- 브랜드 엔터티, 브랜드 인증, 브랜드 문의와 담당/상태
- 광고 문의, 영업 파이프라인, 캠페인 상태
- 사용자 신고, 증거, 대상, 처리 상태, SLA, 이의 제기
- 팝업 저장·상세·외부 링크 등 팝업 퍼널 이벤트
- 관리자 개인 계정, 역할, 마지막 로그인, 세션 목록
- 배포 성공/실패, production 반영 버전, D1 migration 상태
- 개인정보 요청과 삭제/export 처리 이력

### run-report 연동 여부

**`run-report`라는 정확한 이름의 파일·API 연동은 확인되지 않았다.**

대신 보고서 성격의 연동은 다음과 같다.

- `validation-report.json`: 연동됨. 식당 데이터 검증 결과를 읽는다.
- `refresh-report.json`: 연동됨. 최근 개업·폐업과 갱신 시각을 읽는다.
- GitHub workflow runs API: 연동됨. 팝업 및 식당 workflow의 최근 실행 상태를 읽는다.
- 팝업 전용 validation/run report: 확인되지 않음. `popups.json`과 workflow run만 사용한다.

따라서 요구사항의 run-report가 별도 산출물을 뜻한다면 **미연동**으로 판단한다. 실제 product1에 파일이 존재하는지는 product1 저장소 감사로 확인해야 한다.

### D1 사용 방식

- `wrangler.toml`의 `DB` binding이 `mukdang-db` 하나를 가리킨다.
- `ARCHITECTURE.md`는 product1과 같은 D1을 읽고, 스키마·마이그레이션 기준 저장소는 product1이라고 명시한다.
- product2는 D1을 단순 read-only로만 쓰지 않는다. 회원/리뷰/설정/관리자 할 일/로그/auth attempt를 직접 생성·수정·삭제한다.
- 목록은 최대 100건, 기본 50건의 ID 커서 페이지네이션을 사용한다. 이는 확장성과 응답 크기 면에서 장점이다.
- 분석은 D1에서 기간 집계를 직접 수행하며 KST(`+9 hours`) 버킷을 사용한다.
- product2에도 `admin_tasks`, `food_popups`, 인덱스 마이그레이션이 있다. 스키마 기준이 product1인 구조에서 동기화 누락 위험이 있다.
- `food_popups` D1 테이블 마이그레이션은 존재하지만 현재 팝업 화면/API는 이 테이블을 사용하지 않고 product1 JSON을 읽는다. **죽은 모델 또는 전환 중인 모델**로 보이며 진실 원천 혼란을 만든다.
- 회원·리뷰 DELETE는 물리 삭제다. 외래키 cascade, 복구, 개인정보 처리 범위는 product2 코드만으로 확인되지 않는다.

### API 구조

장점:

- 모든 관리자 API가 `/api/admin/*` 아래에 있고 로그인 이후 `requireAdmin`을 일괄 적용한다.
- JSON 응답, `no-store`, request ID, 공통 예외 처리, 입력 ID/검색 검증이 있다.
- D1 쿼리는 bind parameter를 사용한다.
- 커서 페이지네이션과 외부 데이터의 짧은 메모리 캐시가 있다.

문제:

- 600줄 이상의 catch-all 파일 하나가 인증, 회원, 리뷰, 분석, 작업, 팝업, 식당을 모두 담당한다.
- API 버전, OpenAPI 계약, 도메인별 service/repository 경계가 없다.
- 조회와 파괴 작업 모두 단일 `admin: true` 세션이면 가능하다.
- PATCH에 낙관적 버전 검사와 idempotency가 없고, DELETE는 soft delete가 아니다.
- 오류 형식은 대체로 `{error, code?}`이나 code 적용이 일관되지 않다.
- 외부 GitHub/정적 JSON 실패와 stale 데이터 사용 여부를 사용자에게 충분히 설명하지 않는다.
- 팝업/식당 데이터는 D1, GitHub API, 배포 JSON, product2 GitHub 이력을 혼합해 읽는다.

---

## 5. 보안 감사

### 인증

현재 방식:

- `ADMIN_PASSWORD` Secret과 입력 코드를 SHA-256 digest 후 constant-time 비교한다.
- 성공하면 `SESSION_SECRET` HMAC 서명 payload를 `mukdang_admin` 쿠키로 발급한다.
- 쿠키는 `HttpOnly; Secure; SameSite=Strict`, 유효기간 12시간이다.
- IP별 15분 창에서 로그인 실패 8회 제한을 D1 `auth_attempts`에 저장한다.
- 새 탭/브라우저에서는 `sessionStorage` 표시가 없으면 logout API를 호출해 재인증을 요구한다.

평가:

- 비밀번호와 세션 키를 Cloudflare Secrets에 두고 서명 쿠키를 쓰는 기본은 건전하다.
- 그러나 **공유 관리자 코드 하나**로 모든 관리자가 동일한 `admin: true` 주체가 된다.
- MFA, 개별 계정, 역할, 세션 목록/강제 폐기, 비밀번호 교체 이력, 로그인 성공/실패의 사용자별 감사가 없다.
- 로그인 rate limit은 클라이언트 IP 기반이라 공유 IP 오탐과 분산 공격 한계가 있다.
- 세션은 서버 저장 상태가 없는 토큰이므로 Secret 회전 전 개별 세션 폐기가 어렵다.

### 권한

- 관리자 역할은 하나뿐이고 API별 권한 검사가 없다.
- 같은 세션이 분석 열람, 회원 관리자 승격, 회원 삭제, 리뷰 삭제, 설정 변경, workflow 실행을 모두 수행한다.
- product1 회원의 `role=admin`과 product2 관리자 쿠키는 별개이며, 콘솔 접근 주체와 회원 권한 변경 주체가 연결되지 않는다.
- 최소 권한, 2인 승인, 재인증, 고위험 작업 제한이 없다.

### 관리자 URL 노출

- `admin.mukdang.com/`은 `/ops-7c4e91b6`로 308 redirect된다.
- 문서는 `noindex`, `nofollow`, `noarchive`, `nosnippet`, robots `Disallow: /`와 `X-Robots-Tag`를 사용한다.
- URL 난독화와 noindex는 검색 노출을 줄일 뿐 인증 경계가 아니다. 실제 API는 URL을 알아도 인증 전 401로 막히므로 핵심 방어는 서버 인증이다.
- URL이 코드와 배포 자산에 포함될 수 있으므로 비밀로 간주해서는 안 된다.
- 루트 redirect가 관리자 경로를 공개하므로 난독화의 실질 보안 가치는 거의 없다.

### 민감정보

장점:

- Secret 값은 클라이언트에 반환되지 않는다.
- 응답과 전체 사이트에 `no-store`, frame deny, no-referrer 계열 헤더가 있다.
- API middleware는 다른 Origin의 unsafe 요청을 차단하며 SameSite=Strict 쿠키도 사용한다.
- 출력 대부분은 `escapeHtml`로 이스케이프한다.

위험:

- 회원 이메일, 프로필, bio, 선호, 저장 목록, 활동 상세가 하나의 공유 관리자 권한에 넓게 노출된다.
- 민감 데이터 조회 자체는 감사 로그에 남지 않는다.
- 관리자 로그 detail이 자유 문자열이며 향후 민감정보가 들어갈 위험을 통제하는 schema가 없다.
- 회원/리뷰 물리 삭제의 복구와 연관 데이터 정리 범위가 명확하지 않다.
- `Permissions-Policy`, CSP 등 헤더는 양호하지만 CSP에 `style-src 'unsafe-inline'`이 포함된다.
- 사용자별 분석에 실명·이메일이 기본 노출되어 집계 분석 목적 대비 과도할 수 있다.

### 보안 우선 조치

1. 개별 관리자 계정과 MFA를 도입한다.
2. 역할별 최소 권한과 API 권한 매트릭스를 적용한다.
3. 회원 관리자 승격·물리 삭제·대량 작업에 재인증/승인을 적용한다.
4. 감사 로그에 관리자 ID, 대상, 이전/이후, 사유, 요청 ID를 기록한다.
5. 개인정보 화면 접근을 역할로 제한하고 열람 로그를 남긴다.

---

## 6. 운영 효율

### 현재 가능한 일일 운영 루틴 예상

| 단계 | 업무 | 정상일 예상 시간 | 예외일 예상 시간 |
| --- | --- | ---: | ---: |
| 1 | 로그인·Dashboard 확인 | 2분 | 2분 |
| 2 | 팝업 신규/진행/종료와 수집 run 확인 | 6~10분 | 10분 + 외부 수정 시간 |
| 3 | 식당 run·validation·변경 이력 확인 | 4~6분 | 10~30분 이상 GitHub 조사 |
| 4 | 리뷰·회원 이상 확인 | 5~8분 | 신고 원문을 외부에서 찾는 시간 추가 |
| 5 | 운영 로그·해야 할 일 확인 | 3~5분 | 수동 기록 시간 추가 |
| 6 | 문의·신고 처리 | 경로 없음 | 별도 채널에서 무제한 |
| **합계** |  | **20~31분** | **30분 초과 또는 완결 불가** |

정상일 추정은 데이터량이 적고 모든 대상이 첫 50건 또는 현재 화면에 있다는 가정이다. 검수량·문의량·실제 production latency 데이터가 없으므로 절대 시간이 아니라 구조적 비교치다.

### 30분 목표를 막는 요인

- 오늘 처리할 검수·오류·신고·문의의 통합 개수와 우선순위가 없다.
- 팝업 확인 후 수정하려면 product2를 이탈해야 한다.
- 예외 처리의 담당, 상태, SLA와 다음 행동이 없다.
- 분석 메뉴가 일일 운영 메뉴보다 상단에 있어 순차 점검 동선을 늘린다.
- 동일 사용자·활동 정보를 여러 메뉴에서 다시 찾는다.
- “정상” 고정 표시가 실제 이상 탐지를 대신하지 못한다.
- 성공한 workflow와 실제 데이터 반영 성공을 한 단계로 간주할 위험이 있다.

### 자동화 가능한 작업

| 작업 | 현재 상태 | 자동화 방향 |
| --- | --- | --- |
| 날짜 기반 팝업 상태 분류 | UI에서 계산/원본 status 병용 | 서버 기준 상태 전이와 예외 경보로 단일화 |
| 신규 팝업 후보 생성 | `firstSeenAt` 기준 표시 | 필수 필드·신뢰도·중복 검사를 거친 검수 큐 생성 |
| 종료 임박/종료 확인 | 탭 조회 | 종료 전 재검증, 자동 종료, 연장 충돌만 큐 생성 |
| 수집 실패 감지 | workflow run 조회 | 실패/지연/데이터 급변 시 P0 경보와 담당 Task 생성 |
| 식당 변경 이상치 | 75,000건 안전 중단만 있음 | 지역·전체 대비 변화율과 schema 변화를 자동 경보 |
| 중복 후보 | 없음 | 이름·장소·날짜·출처 기반 후보와 안전한 자동 확정 기준 |
| 신고 우선순위 | 없음 | 안전/영업중단/날짜 오류/일반 순으로 SLA 자동 배정 |
| 일일 요약 | 없음 | 미처리·실패·곧 종료·신규·신고를 한 화면/다이제스트로 집계 |
| 분석 보고 | 화면별 수동 조회 | 주간 자동 요약으로 일일 메뉴에서 분리 |

### 반드시 수동 판단이 필요한 작업

- 충돌하는 공식 출처 중 어떤 값을 채택할지 결정
- 브랜드 소유권 및 정정 요청의 증빙 검토
- 유사 행사가 동일 회차인지, 지점별 별개 행사인지 병합 판단
- 명예훼손·개인정보·위법·광고성 리뷰 신고 판단
- 조기 종료, 취소, 연장처럼 날짜 규칙만으로 확정할 수 없는 변경
- 광고 적합성, 표시, 이해관계와 캠페인 승인
- 대량 데이터 이상에서 게시 중단·롤백 결정

자동화의 목표는 수동 판단을 없애는 것이 아니라 운영자가 30분 동안 위 예외에만 집중하게 하는 것이다.

---

## 7. 향후 필요한 관리자 기능 우선순위

이번 감사에서는 구현하지 않는다. 우선순위는 30분 운영 완결성, 사용자 피해, 데이터 복구성 순이다.

### P0 — 일일 운영을 가능하게 하는 기능

1. **통합 운영 Inbox:** 신규 검수, 오류, 신고, 문의, 수집 실패, 곧 종료를 위험도·SLA·담당자 순으로 한 화면에 표시한다.
2. **팝업 승인/반려 흐름:** 원본과 정규값 비교, 필수 필드 검증, 승인·반려·보류 사유, product1 반영 확인을 제공한다.
3. **팝업 편집과 수동 상태 처리:** 잘못된 필드 수정, 취소·조기 종료·연장, source와 근거, 수집 덮어쓰기 방지를 지원한다.
4. **중복 후보와 병합/복구:** 비교, canonical 선택, alias/redirect, 오병합 롤백을 지원한다.
5. **사용자 신고 큐:** 대상, 신고 사유, 증거, 우선순위, 처리 상태, 조치와 통지를 연결한다.
6. **수집 오류 상세:** 소스, 실행, 파서 버전, 실패 원인, 영향 레코드, 재시도와 해결 상태를 보여준다.
7. **개별 관리자 인증과 RBAC:** MFA, 역할, 최소 권한, 세션 폐기와 관리자별 주체 식별을 제공한다.
8. **강화된 감사 로그:** 주체, 대상, 이전/이후 값, 사유, 요청 ID, 결과와 복구 링크를 기록한다.
9. **파괴 작업 안전장치:** 회원·리뷰·데이터의 soft delete, 영향 미리보기, 재인증/승인, 복구를 제공한다.
10. **실제 Health/반영 검증:** hardcoded “정상”을 제거하고 D1, Functions, workflow, 산출물 신선도와 production 버전을 검사한다.

### P1 — 운영 시간을 안정적으로 줄이는 기능

1. 브랜드 문의/정정/소유권 inbox와 담당·SLA·답변 템플릿
2. 광고 문의/리드 관리와 브랜드 문의의 유형 분리
3. 회원·사용자 데이터·활동·리뷰를 한 회원 상세로 통합
4. 분석 3종을 주간 분석 섹션으로 통합하고 일일 메뉴에서 분리
5. 전역 검색: 팝업, 식당, 브랜드, 회원, 리뷰, 신고, 문의 검색
6. 저장된 필터, 대량 선택, 안전한 일괄 처리와 작업 진행 상태
7. 품질 KPI: 필수 필드, 신선도, 중복, 종료 지연, 검수 SLA
8. 설정 변경의 이전/이후 diff, 권한, 영향 설명과 예약 적용
9. 모바일 긴급 운영용 카드/상세 패턴과 접근성 개선
10. 알림: P0 실패·사용자 피해 가능성만 채널별 escalation

### P2 — 성장과 사업 운영

1. 브랜드 인증·팀 계정과 셀프서비스 행사 제출 관리
2. 브랜드 성과 집계와 개인정보 보호 임계값
3. 광고 캠페인, 소재 승인, 라벨, 빈도, 기간 관리
4. SEO 품질: 색인 상태, canonical, sitemap, broken source 경보
5. 파트너/API key, scope, quota, usage, 회전 관리
6. 개인정보 export/delete 요청 처리와 법정 기한 관리
7. UGC 이의 제기, 제재 이력과 반복 위반 탐지
8. 운영 비용, source별 정확도·실패율·처리 시간 분석

### P3 — 플랫폼 확장

1. 다지역·다국어 taxonomy와 지역별 운영 권한
2. 브랜드-공간 매칭과 제휴 workflow
3. 고급 수요/혼잡 예측 검토 화면
4. 기업 고객 SLA·데이터 계약·청구 관리
5. 자동 추천 규칙과 편집 큐레이션 운영

---

## 8. 최종 평가

### 현재 관리자 점수

**54/100 — 조회와 일부 조치는 가능하지만, Mukdang의 핵심 데이터 운영을 완결하지 못한다.**

점수 해석:

- 80~100: 30분 내 일일 운영과 예외 처리가 대부분 가능
- 65~79: 주요 업무 가능, 일부 외부 도구와 수동 연결 필요
- 50~64: 모니터링은 가능하나 핵심 처리 흐름 다수 누락
- 0~49: 일상 운영의 신뢰성과 보안이 부족

현재는 50~64 구간의 중간이다. 회원 정지, 리뷰 숨김, 설정 변경, workflow 재실행은 실용적이지만 서비스의 핵심인 팝업 품질과 운영 커뮤니케이션이 읽기 전용 또는 부재다.

### 장점 Top 10

1. product2의 책임을 관리자 UI·관리자 API로 명시하고 product1과 D1 공유 관계를 문서화했다.
2. 관리자 API 전반을 서버 인증 뒤에 두고 Secret 기반 비밀번호·세션 키를 사용한다.
3. HttpOnly, Secure, SameSite=Strict 서명 쿠키와 12시간 만료를 적용한다.
4. 로그인 실패 rate limit과 timing-safe 비교를 구현했다.
5. noindex, no-store, frame deny, no-referrer, CSP 등 관리자용 보안 헤더가 갖춰져 있다.
6. D1 bind parameter, 입력 ID/검색 검증, 최대 100건 제한과 커서 페이지네이션을 사용한다.
7. 팝업과 식당 workflow의 최근 상태와 수동 재실행을 콘솔에서 확인할 수 있다.
8. 팝업의 진행·예정·종료, 지역·브랜드·검색·정렬과 원문 이동이 있어 일상 모니터링이 빠르다.
9. 식당 변경 이력을 90일 보존하고 상세는 펼칠 때만 로드해 초기 응답을 절약한다.
10. 회원, 리뷰, 사용자 활동, 검색 순위, 설정, 로그까지 기초 운영 범위가 넓고 반응형 레이아웃이 있다.

### 문제 Top 20

1. 신규 팝업 승인 기능이 없다.
2. 팝업 정보 수정 기능이 없다.
3. 조기 종료·취소·연장 등 수동 상태 처리 기능이 없다.
4. 팝업 중복 탐지·병합·롤백 기능이 없다.
5. 사용자 신고 접수·처리 큐가 없다.
6. 브랜드 문의·정정·소유권 문의 기능이 없다.
7. 광고 문의 기능이 없다.
8. 오늘 처리해야 할 예외를 모은 통합 운영 Inbox가 없다.
9. 관리자 인증이 공유 코드 하나이며 개별 관리자와 MFA가 없다.
10. 역할 기반 권한이 없어 모든 관리자가 삭제·권한 상승·설정·workflow 실행을 할 수 있다.
11. 감사 로그에 관리자 ID와 이전/이후 값, 사유, 대상 링크가 없다.
12. 회원과 리뷰가 물리 삭제되어 복구·이의 처리·영향 확인이 어렵다.
13. Dashboard의 여러 “정상” 상태가 실제 health check가 아닌 고정 텍스트다.
14. `food_popups` D1 테이블은 존재하지만 실제 화면은 product1 JSON을 사용해 진실 원천이 혼란스럽다.
15. D1, GitHub API, product1 배포 JSON, product2 GitHub JSON이 혼합되어 실패 시 데이터 일관성을 판단하기 어렵다.
16. 식당 validation은 error/warning 개수만 반환하고 실제 오류 항목과 처리 흐름을 제공하지 않는다.
17. 팝업 전용 수집 품질·파서 오류·필드 충돌 보고서가 없다.
18. 분석·사용자 메뉴가 중복되고 12개 최상위 메뉴가 평면으로 배치되어 일일 운영 순서가 없다.
19. 관리자 catch-all API가 600줄 이상 한 파일에 집중되고 핵심 API 통합/권한 테스트가 없다.
20. 모바일은 조회 가능하지만 넓은 표, 오른쪽 끝 작업 버튼, 메뉴 접근성 때문에 실제 검수 작업이 비효율적이다.

### 가장 먼저 고쳐야 하는 것 Top 10

1. **통합 운영 Inbox를 정의한다:** 오늘의 신규 검수, 오류, 신고, 문의, 실패, 종료 예외를 한곳에 모은다.
2. **팝업 진실 원천을 하나로 확정한다:** D1 `food_popups`와 product1 `popups.json` 중 writer/read model 책임과 전환 계획을 결정한다.
3. **팝업 승인·수정·상태 전이를 만든다:** 근거와 감사 이력을 포함해 product2에서 업무를 완결한다.
4. **사용자 신고와 브랜드 문의 큐를 만든다:** 대상·담당·상태·SLA·처리 결과를 추적한다.
5. **중복 병합과 롤백을 만든다:** canonical 레코드와 URL/출처 이력을 보존한다.
6. **개별 관리자 계정, MFA, RBAC를 도입한다:** 공유 코드와 전권 세션을 제거한다.
7. **감사 로그를 증거 가능한 변경 이력으로 강화한다:** 관리자, 전후 값, 사유, 요청 ID와 복구를 연결한다.
8. **회원·리뷰 물리 삭제를 안전한 비활성/soft delete 흐름으로 바꾼다:** 영향 미리보기와 복구를 제공한다.
9. **Dashboard의 고정 정상 문구를 실제 신선도·실패·반영 검증으로 대체한다:** 운영자가 거짓 안정감을 갖지 않게 한다.
10. **분석과 사용자 메뉴를 통합하고 일일 운영 순서대로 재구성한다:** 30분을 검수와 예외 판단에 사용하게 한다.

---

## 결론

product2는 작은 팀이 회원·리뷰와 수집 상태를 빠르게 살피는 초기 관리자 도구로는 좋은 기반이다. 보안 헤더, 서버 인증, D1 입력 검증, 페이지네이션, GitHub Actions 연결 같은 기초도 이미 갖추고 있다.

하지만 Mukdang 운영의 핵심 단위인 “팝업 예외 한 건”을 발견한 뒤 승인·수정·종료·병합하고 근거와 결과를 남기는 폐쇄 루프가 없다. 문의와 신고도 시스템 밖에 있다. 이 상태에서 30분이라는 목표는 **아무 문제도 없는 날의 점검 시간**으로는 달성 가능하지만 **실제 운영 완결 시간**으로는 달성할 수 없다.

다음 개발의 출발점은 메뉴나 차트를 늘리는 것이 아니라, 통합 운영 Inbox와 팝업 데이터의 단일 진실 원천을 확정하는 것이다. 그 위에 승인·정정·병합·신고·문의의 처리 상태와 감사 이력을 연결해야 운영자는 하루 30분 동안 반복 조회가 아닌 고가치 판단에 집중할 수 있다.
